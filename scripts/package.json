{
  "name": "scripts",
  "version": "1.0.0",
  "private": true,
  "description": "Utility scripts for concatenating documentation",
  "license": "ISC",
  "author": "FIBEX",
  "type": "commonjs",
  "scripts": {
    "concat": "ts-node -P ./tsconfig.json ./concat-docs.ts",
    "build": "tsc -P ./tsconfig.json",
    "concat:node": "npm run build && node ./dist/concat-docs.js",
    "serve": "ts-node -P ./tsconfig.json ./serve.ts",
    "serve:node": "npm run build && node ./dist/serve.js"
  },
  "dependencies": {
    "marked": "^12.0.2"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
